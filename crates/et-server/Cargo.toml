[package]
name = "et-server"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "EternalTerminal server side: `etserver` daemon and `etterminal` (PTY host), wire-compatible with upstream C++."

[features]
# Hook for cross-crate integration tests (et-client tests spin up the whole
# stack in-process). Currently a marker; all testing entry points are plain
# `pub`.
testing = []

[dependencies]
et-proto.workspace = true
prost.workspace = true
thiserror.workspace = true
tokio.workspace = true
nix.workspace = true
libc.workspace = true
