[package]
name = "et-proto-gen"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false
description = "Manual generator for et-proto's committed protobuf code (protoc + buffa-build)."

[dependencies]
buffa-build.workspace = true
